spring:
  application:
    name: room-service

  datasource:
    username: sa
    password: password
    url: jdbc:h2:mem:testdb
  jpa:
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true
      path: /h2-console

  grpc:
    client:
      channel:
        role-service:
          target: static://localhost:9090
          ssl:
            enabled: true
            bundle: lokit-room-client-ssl
    server:
      port: 9091
      ssl:
        enabled: true
        client-auth: require
        bundle: lokit-room-server-ssl

  ssl:
    bundle:
      jks:
        lokit-room-server-ssl:
          keystore.location: classpath:room-server-keystore.p12
          keystore.password: changeit
          keystore.type: PKCS12
          truststore.location: classpath:room-server-truststore.p12
          truststore.password: changeit
          truststore.type: PKCS12
        lokit-room-client-ssl:
          keystore.location: classpath:room-client-keystore.p12
          keystore.password: changeit
          keystore.type: PKCS12
          truststore.location: classpath:room-client-truststore.p12
          truststore.password: changeit
          truststore.type: PKCS12

  rabbitmq:
    username: user
    password: password
    host: localhost
    port: 5672

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8090/realms/lokit
          jwk-set-uri: http://localhost:8090/realms/lokit/protocol/openid-connect/certs

server:
  port: 8081
