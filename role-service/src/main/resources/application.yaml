spring:
  application:
    name: role-service
  datasource:
    username: sa
    password: password
    url: jdbc:h2:mem:testdb
  jpa:
    hibernate:
      ddl-auto: create-drop

  h2:
    console:
      enabled: true
      path: /h2-console

  rabbitmq:
    username: user
    password: password
    host: localhost
    port: 5672