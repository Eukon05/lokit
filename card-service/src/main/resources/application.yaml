spring:
  application:
    name: card-service

  datasource:
    username: ${LOKIT_CARD_DB_USERNAME}
    password: ${LOKIT_CARD_DB_PASSWORD}
    url: ${LOKIT_CARD_DB_URL}
  jpa:
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true
      path: /h2-console

  rabbitmq:
    username: ${LOKIT_RABBITMQ_USERNAME}
    password: ${LOKIT_RABBITMQ_PASSWORD}
    host: ${LOKIT_RABBITMQ_HOST}
    port: ${LOKIT_RABBITMQ_PORT}

  grpc:
    server:
      port: ${LOKIT_CARD_GRPC_PORT}
      ssl:
        enabled: true
        client-auth: require
        bundle: lokit-card-server-ssl
    client:
      channel:
        identity-service:
          target: ${LOKIT_IDENTITY_GRPC_TARGET}
          ssl:
            enabled: true
            bundle: lokit-card-client-ssl

  ssl:
    bundle:
      jks:
        lokit-card-server-ssl:
          keystore.location: classpath:card-server-keystore.p12
          keystore.password: ${LOKIT_CARD_TLS_SERVER_KEYSTORE_PASSWORD}
          keystore.type: PKCS12
          truststore.location: classpath:card-server-truststore.p12
          truststore.password: ${LOKIT_CARD_TLS_SERVER_TRUSTSTORE_PASSWORD}
          truststore.type: PKCS12
        lokit-card-client-ssl:
          keystore.location: classpath:card-client-keystore.p12
          keystore.password: ${LOKIT_CARD_TLS_CLIENT_KEYSTORE_PASSWORD}
          keystore.type: PKCS12
          truststore.location: classpath:card-client-truststore.p12
          truststore.password: ${LOKIT_CARD_TLS_CLIENT_TRUSTSTORE_PASSWORD}
          truststore.type: PKCS12

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${LOKIT_OAUTH_ISSUER_URI}
          jwk-set-uri: ${LOKIT_OAUTH_JWK_URI}

eureka:
  client:
    service-url:
      defaultZone: ${LOKIT_EUREKA_HOST}:${LOKIT_EUREKA_PORT}/eureka
  instance:
    prefer-ip-address: true
    metadata-map:
      grpc_port: ${spring.grpc.server.port}

server:
  port: ${LOKIT_CARD_PORT}

lokit:
  jwt:
    role-claims: ${LOKIT_JWT_ROLE_CLAIMS}
